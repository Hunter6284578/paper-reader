# Use a single-owner device-pairing model

Paper Reader is a private installation for one Owner, not a multi-tenant account system. Clients authenticate as revocable Paired Devices; papers, vocabulary, reading history, and model settings remain installation-wide. The unused username registration and login path is removed so the authentication Interface no longer promises data isolation the schema does not provide.
